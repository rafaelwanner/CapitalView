import React from 'react';



function Stats(data) {

  //const assets = data.data.map(item => <Asset key={item.class} title={item.class} />)


  return(
    <div>
      <h1>Stats</h1>
    </div>
  )
}

export default Stats;
